---
// src/components/Main_RIDDLE.astro
---
<main class="app-page">
  <section class="section hero" id="overview">
    <div class="hero-grid">
      <div class="hero-copy">
        <span class="hero-eyebrow">クイズ✖️SNSプラットフォーム</span>
        <h1 class="hero-title">Riddle</h1>
        <p class="hero-subtitle">クイズ✖️SNSで、難問とコミュニティの両方から刺激を受ける新感覚の脳トレ体験。解いた瞬間を仲間と共有して盛り上がろう。</p>
        <div class="hero-meta">
          <span class="meta-chip">クイズ✖️SNS</span>
          <span class="meta-chip">ライブランキング</span>
          <span class="meta-chip">コミュニティ投稿</span>
        </div>
        <div class="store-buttons">
          <a href="https://apps.apple.com/jp/app/riddle/id1621658101" class="store-button store-button--app-store" target="_blank" rel="noopener noreferrer">
            <img src="/Download_on_the_App_Store_Badge_JP_RGB_blk_100317.svg" alt="App Storeでダウンロード" class="store-badge" loading="lazy" />
          </a>
          <a href="https://play.google.com/store/apps/details?id=com.higuchiyuya.riddle" class="store-button store-button--google" target="_blank" rel="noopener noreferrer">
            <img src="/Riddle/GetItOnGooglePlay_Badge_Web_color_Japanese.png" alt="Google Playで入手" class="store-badge" loading="lazy" />
          </a>
        </div>
        <div class="hero-actions">
          <a href="#features" class="primary-action">主な機能を見る</a>
          <a href="/contact" class="ghost-action">お問い合わせ</a>
        </div>
      </div>
      <div class="hero-visual" aria-hidden="true">
        <div class="glass-panel">
          <div class="panel-accent"></div>
          <img src="/RiddleLogo.png" alt="Riddle app visual" class="hero-image" />
        </div>
      </div>
    </div>
  </section>

  <section class="section" id="features">
    <div class="section-header">
      <h2>Riddleで鍛える3つの力</h2>
      <p>多彩なジャンルと難易度で、飽きることなく脳をストレッチ。毎日の隙間時間を、知的なトレーニングに変えます。</p>
    </div>
    <div class="feature-grid">
      <article class="feature-card">
        <span class="feature-icon">🧩</span>
        <h3>豊富なパズルライブラリ</h3>
        <p>ロジック、数学、言語など、多面的に思考を刺激する問題を収録。アップデートで新問題も続々追加。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">🎯</span>
        <h3>段階的な難易度</h3>
        <p>初級から上級まで細かなレベル設定。自分のペースに合わせてステップアップできます。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">📊</span>
        <h3>成長を可視化</h3>
        <p>正答率や挑戦履歴を記録し、得意・苦手を分析。振り返りで次のゴールが明確になります。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">🔥</span>
        <h3>コミュニティタイムライン</h3>
        <p>解答のひらめきや感想を投稿してSNSのように交流。お気に入りユーザーの挑戦もタイムラインでチェックできます。</p>
      </article>
    </div>
  </section>

  <section class="section highlight" id="community">
    <div class="plan-card">
      <div class="plan-badge">Quiz ✖️ Social</div>
      <h2>自分に合ったスタイルで継続できる</h2>
      <p>短時間でも気軽に挑戦でき、SNSの盛り上がりが継続のモチベーションに。通勤や休憩時間に仲間と同じ問題で盛り上がれます。</p>
      <ul>
        <li>カテゴリ別の問題で苦手分野を重点強化</li>
        <li>SNSタイムラインでひらめきを共有しヒントを得られる</li>
        <li>ランキングで全国のユーザーと記録を競える</li>
      </ul>
      <div class="plan-actions">
        <a href="https://apps.apple.com/jp/app/riddle/id1621658101" class="primary-action" target="_blank" rel="noopener noreferrer">今すぐプレイ</a>
        <a href="https://play.google.com/store/apps/details?id=com.higuchiyuya.riddle" class="ghost-action" target="_blank" rel="noopener noreferrer">Google Playでみる</a>
      </div>
    </div>
  </section>

  <section class="section cta">
    <div class="cta-card">
      <h2>ひらめきと論理を磨く毎日を</h2>
      <p>Riddleがあれば、いつでもどこでも頭のストレッチ。新しい発想が必要なとき、ひとつの謎解きが突破口になります。</p>
      <div class="cta-actions">
        <a href="https://apps.apple.com/jp/app/riddle/id1621658101" class="primary-action" target="_blank" rel="noopener noreferrer">App Storeでダウンロード</a>
        <a href="https://play.google.com/store/apps/details?id=com.higuchiyuya.riddle" class="ghost-action" target="_blank" rel="noopener noreferrer">Google Playで入手</a>
      </div>
    </div>
  </section>
</main>

<style>
  .app-page {
    padding-top: 96px;
    background: linear-gradient(180deg, #111111 0%, #0b0b0b 100%);
    color: #111111;
    font-family: 'Inter', 'Hiragino Sans', 'Hiragino Kaku Gothic ProN', 'Noto Sans JP', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  .section {
    padding: 5rem 1.75rem;
  }

  .section + .section {
    border-top: 1px solid rgba(255, 255, 255, 0.04);
  }

  .hero {
    background: linear-gradient(180deg, #090909 0%, #161616 100%);
    color: #f5f5f5;
    padding-bottom: 4.5rem;
  }

  .hero-grid {
    max-width: 1120px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 3.5rem;
    align-items: center;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .hero-eyebrow {
    font-size: 0.85rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: rgba(245, 245, 245, 0.6);
  }

  .hero-title {
    font-size: clamp(3rem, 6vw, 4.5rem);
    font-weight: 800;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .hero-subtitle {
    font-size: 1.1rem;
    line-height: 1.9;
    color: rgba(229, 229, 229, 0.82);
    margin: 0;
  }

  .hero-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .meta-chip {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    border: 1px solid rgba(245, 245, 245, 0.3);
    color: rgba(245, 245, 245, 0.85);
    padding: 0.35rem 1rem;
    font-size: 0.85rem;
    letter-spacing: 0.02em;
  }

  .store-buttons {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
  }

  .store-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    border: none;
    background: transparent;
    border-radius: 14px;
    overflow: hidden;
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.25);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .store-button--app-store,
  .store-button--google {
    background: #ffffff;
    border: 1px solid rgba(0, 0, 0, 0.06);
  }

  .store-button.disabled {
    opacity: 0.55;
    cursor: default;
    pointer-events: none;
    box-shadow: none;
  }

  .store-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 16px 32px rgba(0, 0, 0, 0.35);
  }

  .store-badge {
    display: block;
    height: 52px;
    width: auto;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .primary-action,
  .ghost-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 1.9rem;
    border-radius: 999px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease, border-color 0.2s ease, color 0.2s ease;
  }

  .primary-action {
    background: #f5f5f5;
    color: #111111;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.35);
  }

  .primary-action:hover {
    transform: translateY(-2px);
    box-shadow: 0 22px 45px rgba(0, 0, 0, 0.45);
  }

  .ghost-action {
    background: transparent;
    color: rgba(245, 245, 245, 0.9);
    border: 1px solid rgba(245, 245, 245, 0.35);
  }

  .ghost-action:hover {
    background: rgba(245, 245, 245, 0.08);
    transform: translateY(-2px);
    border-color: rgba(245, 245, 245, 0.5);
  }

  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .glass-panel {
    position: relative;
    width: min(420px, 100%);
    border-radius: 32px;
    padding: 2.5rem 2.25rem;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(24px);
  }

  .panel-accent {
    position: absolute;
    inset: 12%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.25), transparent 75%);
    filter: blur(18px);
    z-index: 0;
  }

  .hero-image {
    position: relative;
    width: 100%;
    border-radius: 24px;
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.45);
    z-index: 1;
  }

  .section-header {
    max-width: 760px;
    margin: 0 auto 3rem;
    text-align: center;
    color: #f5f5f5;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 1rem;
    font-weight: 800;
  }

  .section-header p {
    font-size: 1.05rem;
    color: rgba(229, 229, 229, 0.75);
    margin: 0;
    line-height: 1.8;
  }

  .feature-grid {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.75rem;
  }

  .feature-card {
    background: #fefefe;
    border-radius: 24px;
    padding: 2rem;
    border: 1px solid rgba(17, 17, 17, 0.08);
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.08);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    color: #111111;
  }

  .feature-card h3 {
    margin: 0;
    font-size: 1.2rem;
    font-weight: 700;
  }

  .feature-card p {
    margin: 0;
    color: rgba(51, 51, 51, 0.85);
    line-height: 1.7;
    font-size: 0.98rem;
  }

  .feature-icon {
    font-size: 2.5rem;
    line-height: 1;
  }

  .highlight {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(10, 10, 10, 0.2));
    color: #f5f5f5;
  }

  .plan-card {
    max-width: 920px;
    margin: 0 auto;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 28px;
    padding: 3rem;
    border: 1px solid rgba(255, 255, 255, 0.18);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.35);
  }

  .plan-badge {
    display: inline-flex;
    padding: 0.35rem 0.9rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.12);
    color: #f5f5f5;
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .plan-card h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.4rem);
    margin: 1.5rem 0 1rem;
    font-weight: 800;
  }

  .plan-card p {
    font-size: 1.05rem;
    line-height: 1.8;
    color: rgba(229, 229, 229, 0.78);
    margin: 0 0 1.75rem;
  }

  .plan-card ul {
    list-style: none;
    padding: 0;
    margin: 0 0 2.5rem;
    display: grid;
    gap: 0.9rem;
  }

  .plan-card li {
    font-size: 1rem;
    color: #f5f5f5;
    padding-left: 1.75rem;
    position: relative;
  }

  .plan-card li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    top: 0.1rem;
    font-weight: 700;
    color: #f5f5f5;
  }

  .plan-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .cta {
    background: linear-gradient(180deg, #080808 0%, #141414 100%);
    color: #f5f5f5;
  }

  .cta-card {
    max-width: 820px;
    margin: 0 auto;
    background: rgba(17, 17, 17, 0.6);
    border-radius: 32px;
    padding: 3rem;
    box-shadow: 0 30px 70px rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(18px);
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .cta-card h2 {
    font-size: clamp(2rem, 4vw, 2.8rem);
    margin-bottom: 1.2rem;
    font-weight: 800;
  }

  .cta-card p {
    font-size: 1.05rem;
    line-height: 1.9;
    margin: 0 auto 2rem;
    max-width: 560px;
    color: rgba(229, 229, 229, 0.75);
  }

  .cta-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  @media (max-width: 1024px) {
    .section {
      padding: 4.5rem 1.5rem;
    }
    .plan-card {
      padding: 2.5rem 2rem;
    }
  }

  @media (max-width: 768px) {
    .app-page {
      padding-top: 88px;
    }
    .hero-grid {
      gap: 2.5rem;
    }
    .store-buttons,
    .hero-actions,
    .plan-actions,
    .cta-actions {
      flex-direction: column;
      align-items: stretch;
    }
    .store-button,
    .primary-action,
    .ghost-action {
      width: 100%;
      justify-content: center;
    }
    .store-badge {
      width: 100%;
      height: auto;
    }
    .glass-panel {
      padding: 2rem 1.75rem;
    }
  }

  @media (max-width: 540px) {
    .section {
      padding: 4rem 1.25rem;
    }
    .feature-card {
      padding: 1.75rem;
    }
    .plan-card {
      padding: 2.25rem 1.5rem;
    }
    .cta-card {
      padding: 2.5rem 1.5rem;
    }
  }
</style>
