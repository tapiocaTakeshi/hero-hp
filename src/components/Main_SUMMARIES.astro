---
// src/components/Main_SUMMARIES.astro
---
<main class="app-page">
  <section class="section hero" id="overview">
    <div class="hero-grid">
      <div class="hero-copy">
        <span class="hero-eyebrow">要約✖️SNSプラットフォーム</span>
        <h1 class="hero-title">Summaries</h1>
        <p class="hero-subtitle">要約✖️SNSで、情報の本質を素早く把握して仲間と共有。長文をAIが要約し、チームで気づきを交換できる新しいナレッジ体験です。</p>
        <div class="hero-meta">
          <span class="meta-chip">要約✖️SNS</span>
          <span class="meta-chip">ナレッジ共有</span>
          <span class="meta-chip">チーム連携</span>
        </div>
        <div class="hero-actions">
          <a href="#features" class="primary-action">主な機能を見る</a>
          <a href="/contact" class="ghost-action">導入について相談</a>
        </div>
      </div>
      <div class="hero-visual" aria-hidden="true">
        <div class="glass-panel">
          <div class="panel-accent"></div>
          <img src="/SummariesLogo.png" alt="Summaries app visual" class="hero-image" />
        </div>
      </div>
    </div>
  </section>

  <section class="section" id="features">
    <div class="section-header">
      <h2>Summariesでできること</h2>
      <p>膨大な情報を扱うワークフローをシンプルに。必要な情報だけを抽出し、理解と意思決定のスピードを高めます。</p>
    </div>
    <div class="feature-grid">
      <article class="feature-card">
        <span class="feature-icon">📚</span>
        <h3>高度なAI要約エンジン</h3>
        <p>長文記事やドキュメントから、重要なポイントを自動抽出。読み手が理解しやすい構造で提示します。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">⚡</span>
        <h3>高速処理</h3>
        <p>多数の資料も短時間で要約。作業時間を大幅に削減し、次のアクションに素早く移れます。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">🎯</span>
        <h3>カスタム要約</h3>
        <p>要約の長さや視点を調整し、目的に合わせたアウトプットを生成。チームで共有しやすい形式で保存できます。</p>
      </article>
      <article class="feature-card">
        <span class="feature-icon">🌐</span>
        <h3>共有タイムライン</h3>
        <p>要約結果をSNSのように投稿してディスカッション。チームの知見がタイムライン上で蓄積されます。</p>
      </article>
    </div>
  </section>

  <section class="section highlight" id="workflow">
    <div class="plan-card">
      <div class="plan-badge">Summary ✖️ Social</div>
      <h2>情報インプットの負荷を減らす</h2>
      <p>Summariesは、調査レポートや学習ノート、社内ナレッジを要約しながらSNS感覚で共有。理解のスピードを揃え、意思決定までの時間を短縮します。</p>
      <ul>
        <li>重要な要約とキーワードを自動でエクスポート</li>
        <li>共有タイムラインで要約とコメントを同時に共有</li>
        <li>学習ログとして蓄積し、後から参照</li>
      </ul>
      <div class="plan-actions">
        <a href="/contact" class="primary-action">導入を相談</a>
        <a href="/summaries_app_description" class="ghost-action">アプリの詳細へ</a>
      </div>
    </div>
  </section>

  <section class="section cta">
    <div class="cta-card">
      <h2>情報の海から、答えだけを受け取る</h2>
      <p>Summariesがあれば、読むべき箇所が一目でわかります。情報の洪水に時間を奪われず、本当に大切な思考に集中しましょう。</p>
      <div class="cta-actions">
        <a href="/summaries_app_description" class="primary-action">詳細を見る</a>
        <a href="/contact" class="ghost-action">お問い合わせ</a>
      </div>
    </div>
  </section>
</main>

<style>
  .app-page {
    padding-top: 96px;
    background: linear-gradient(180deg, #111111 0%, #0b0b0b 100%);
    color: #111111;
    font-family: 'Inter', 'Hiragino Sans', 'Hiragino Kaku Gothic ProN', 'Noto Sans JP', system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  .section {
    padding: 5rem 1.75rem;
  }

  .section + .section {
    border-top: 1px solid rgba(255, 255, 255, 0.04);
  }

  .hero {
    background: linear-gradient(180deg, #090909 0%, #161616 100%);
    color: #f5f5f5;
    padding-bottom: 4.5rem;
  }

  .hero-grid {
    max-width: 1120px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 3.5rem;
    align-items: center;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .hero-eyebrow {
    font-size: 0.85rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(245, 245, 245, 0.6);
  }

  .hero-title {
    font-size: clamp(3rem, 6vw, 4.5rem);
    font-weight: 800;
    margin: 0;
    letter-spacing: -0.02em;
  }

  .hero-subtitle {
    font-size: 1.1rem;
    line-height: 1.9;
    color: rgba(229, 229, 229, 0.82);
    margin: 0;
  }

  .hero-meta {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .meta-chip {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    border: 1px solid rgba(245, 245, 245, 0.3);
    color: rgba(245, 245, 245, 0.85);
    padding: 0.35rem 1rem;
    font-size: 0.85rem;
    letter-spacing: 0.02em;
  }

  .hero-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .glass-panel {
    position: relative;
    width: min(420px, 100%);
    border-radius: 32px;
    padding: 2.5rem 2.25rem;
    background: rgba(255, 255, 255, 0.06);
    border: 1px solid rgba(255, 255, 255, 0.12);
    box-shadow: 0 30px 80px rgba(0, 0, 0, 0.45);
    backdrop-filter: blur(24px);
  }

  .panel-accent {
    position: absolute;
    inset: 12%;
    background: radial-gradient(circle, rgba(255, 255, 255, 0.25), transparent 75%);
    filter: blur(18px);
    z-index: 0;
  }

  .hero-image {
    position: relative;
    width: 100%;
    border-radius: 24px;
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.45);
    z-index: 1;
  }

  .primary-action,
  .ghost-action {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.9rem 1.9rem;
    border-radius: 999px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease, background-color 0.2s ease, border-color 0.2s ease, color 0.2s ease;
  }

  .primary-action {
    background: #f5f5f5;
    color: #111111;
    box-shadow: 0 18px 40px rgba(0, 0, 0, 0.35);
  }

  .primary-action:hover {
    transform: translateY(-2px);
    box-shadow: 0 22px 45px rgba(0, 0, 0, 0.45);
  }

  .ghost-action {
    background: transparent;
    color: rgba(245, 245, 245, 0.9);
    border: 1px solid rgba(245, 245, 245, 0.35);
  }

  .ghost-action:hover {
    background: rgba(245, 245, 245, 0.08);
    transform: translateY(-2px);
    border-color: rgba(245, 245, 245, 0.5);
  }

  .section-header {
    max-width: 760px;
    margin: 0 auto 3rem;
    text-align: center;
    color: #f5f5f5;
  }

  .section-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    margin-bottom: 1rem;
    font-weight: 800;
  }

  .section-header p {
    font-size: 1.05rem;
    color: rgba(229, 229, 229, 0.75);
    margin: 0;
    line-height: 1.8;
  }

  .feature-grid {
    max-width: 1100px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.75rem;
  }

  .feature-card {
    background: #fefefe;
    border-radius: 24px;
    padding: 2rem;
    border: 1px solid rgba(17, 17, 17, 0.08);
    box-shadow: 0 18px 45px rgba(0, 0, 0, 0.08);
    display: flex;
    flex-direction: column;
    gap: 1rem;
    color: #111111;
  }

  .feature-card h3 {
    margin: 0;
    font-size: 1.2rem;
    font-weight: 700;
  }

  .feature-card p {
    margin: 0;
    color: rgba(51, 51, 51, 0.85);
    line-height: 1.7;
    font-size: 0.98rem;
  }

  .feature-icon {
    font-size: 2.5rem;
    line-height: 1;
  }

  .highlight {
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.1), rgba(10, 10, 10, 0.2));
    color: #f5f5f5;
  }

  .plan-card {
    max-width: 920px;
    margin: 0 auto;
    background: rgba(255, 255, 255, 0.08);
    border-radius: 28px;
    padding: 3rem;
    border: 1px solid rgba(255, 255, 255, 0.18);
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.35);
  }

  .plan-badge {
    display: inline-flex;
    padding: 0.35rem 0.9rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.12);
    color: #f5f5f5;
    font-weight: 700;
    font-size: 0.85rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .plan-card h2 {
    font-size: clamp(1.8rem, 3.5vw, 2.4rem);
    margin: 1.5rem 0 1rem;
    font-weight: 800;
  }

  .plan-card p {
    font-size: 1.05rem;
    line-height: 1.8;
    color: rgba(229, 229, 229, 0.78);
    margin: 0 0 1.75rem;
  }

  .plan-card ul {
    list-style: none;
    padding: 0;
    margin: 0 0 2.5rem;
    display: grid;
    gap: 0.9rem;
  }

  .plan-card li {
    font-size: 1rem;
    color: #f5f5f5;
    padding-left: 1.75rem;
    position: relative;
  }

  .plan-card li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    top: 0.1rem;
    font-weight: 700;
    color: #f5f5f5;
  }

  .plan-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .cta {
    background: linear-gradient(180deg, #080808 0%, #141414 100%);
    color: #f5f5f5;
  }

  .cta-card {
    max-width: 820px;
    margin: 0 auto;
    background: rgba(17, 17, 17, 0.6);
    border-radius: 32px;
    padding: 3rem;
    box-shadow: 0 30px 70px rgba(0, 0, 0, 0.5);
    backdrop-filter: blur(18px);
    text-align: center;
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .cta-card h2 {
    font-size: clamp(2rem, 4vw, 2.8rem);
    margin-bottom: 1.2rem;
    font-weight: 800;
  }

  .cta-card p {
    font-size: 1.05rem;
    line-height: 1.9;
    margin: 0 auto 2rem;
    max-width: 560px;
    color: rgba(229, 229, 229, 0.75);
  }

  .cta-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1rem;
  }

  @media (max-width: 1024px) {
    .section {
      padding: 4.5rem 1.5rem;
    }
    .plan-card {
      padding: 2.5rem 2rem;
    }
  }

  @media (max-width: 768px) {
    .app-page {
      padding-top: 88px;
    }
    .hero-grid {
      gap: 2.5rem;
    }
    .hero-actions,
    .plan-actions,
    .cta-actions {
      flex-direction: column;
      align-items: stretch;
    }
    .primary-action,
    .ghost-action {
      width: 100%;
      justify-content: center;
    }
    .glass-panel {
      padding: 2rem 1.75rem;
    }
  }

  @media (max-width: 540px) {
    .section {
      padding: 4rem 1.25rem;
    }
    .feature-card {
      padding: 1.75rem;
    }
    .plan-card {
      padding: 2.25rem 1.5rem;
    }
    .cta-card {
      padding: 2.5rem 1.5rem;
    }
  }
</style>
